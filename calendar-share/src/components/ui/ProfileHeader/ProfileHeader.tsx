import styles from './styles.module.css';

const ProfileHeader = () => {
  return (
    <div className={styles.profileWrapper}>
      <p className={styles.profileTitle}>設定</p>
    </div>
  );
};
export default ProfileHeader;
